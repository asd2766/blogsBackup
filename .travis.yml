language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo d -g

after_script:
  - cd ./public
  - git init
  - git config user.name "asd2766"
  - git config user.email "270092797@qq.com"
  - git add .
  - git commit -m "Update blogs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source
env:
 global:
   - GH_REF: git@github.com:asd2766/blogs.git